// Wap to print addition of two user given matrix

#include<stdio.h>
#include<stdlib.h>

int main() {
    int r1,c1,r2,c2;
    SET_RCOL: printf("Enter the number of rows and columns of first matrix: ");
    scanf("%d%d",&r1,&c1);
    printf("Enter the number of rows and columns of second matrix: ");
    scanf("%d%d",&r2,&c2);
    if(r1==r2 && c1==c2){
        //Dynamic memory allocation
        
    }

    return 0;
}