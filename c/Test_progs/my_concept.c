#include<stdio.h>
#define size 100;
float max(float NumArr[size]);
int main()
{
    float arr[size];
    printf("\n\tEnter the elements :");
    for()
    return(printf("\n\tPrgram ended..you can close the window safely now"));
}
max(float numarr{})
{
    
}